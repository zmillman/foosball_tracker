.spacer
.spacer
  

.row
  .span8.offset2
    %h3 Hey look! It's Zach's
    .spacer2
    %h1.wordmark
      Foosball Tracker!
        
    .section
      It's pretty much the best way to keep track of who's the best
    .section
      - User.all.each do |user|
        = image_tag(user.gravatar_url(:size => 32))
    .spacer
    .section
      = link_to 'Sounds neat, I want to join', sign_up_path, :class => 'btn btn-primary'
      = link_to 'I already have an account', login_path, :class => 'btn'
