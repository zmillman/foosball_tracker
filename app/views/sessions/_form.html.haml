= form_for resource, :html => {:class => 'stacked'} do |f|
  .field
    = f.label :email
    = f.text_field :email
    
  .field
    = f.label :password
    = f.text_field :password 
  
  .field
    = f.submit 'Sign in'
    %span or #{link_to 'sign up', sign_up_path}